# Baked into the bundle at build time by Vite. Not a secret -- the API is
# public and guarded by the passcode, which is never committed.
VITE_API_BASE=https://0xz7v8yx0i.execute-api.us-east-1.amazonaws.com
